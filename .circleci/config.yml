version: 2.1

workflows:
  build-deploy:
    jobs:
      - build


jobs:
  build:
    docker:
      - image: cimg/node:16.18.1
    steps:
      - checkout
      - run:
          name: Install dependecies
          command: npm install
      - run:
          name: Build
          command: npm run build
