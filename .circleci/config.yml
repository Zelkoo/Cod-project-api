version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/base:stable
        auth:
          # ensure you have first added these secrets
          # visit app.circleci.com/settings/project/github/Zelkoo/Cod-project-api/environment-variables
          username: $DOCKER_HUB_USER
          password: $DOCKER_HUB_PASSWORD
jobs:
  unit-test:
    executor: build_and_test
    steps:
      - checkout
      - run: |
          # echo Hello, World!

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
